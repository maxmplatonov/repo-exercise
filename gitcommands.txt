SET UP AND INITIALIZATION

git clone       --add online repository to local
git init        --initialize new repository
git remote -v   --show current remote directory
git status      --show status
git add file.txt -add file
git add.        --add all
git add -A      --add all subderictories
git reset       --my_script.py You can remove a file from staging 

COMMITING
git commit -m "Commit message"
git commit -am "Commit message"     --You can condense staging all tracked files by committing them in one step
git commit --amend -m "New commit message" --If you need to modify your commit message, you can do so with the --amend flag:

BRANCHES
git branch
git branch new-branch
git checkout another-branch
git checkout -b new-branch
git branch -m current-branch-name new-branch-name
git merge branch-name

CALABORATING AND UPDATE!
git fetch upstream
git merge upstream/main
git push origin main   --Push or transmit your local branch commits to the remote repository branch:   
git pull               --Fetch and merge any commits from the tracking remote branch:






